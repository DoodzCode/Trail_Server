pub fn action_processor() {
    // the context here is that all assignments *should* be complete
    // and now the results will be calculated

    // cycle through the person list in the parties manifest
    // if the player has an assignment, calculate the result using a function

    // fn get_wood(person, biome_woods, d20) -> yeild;
    // fn add_wood(wagon, yeild);
    // person::state = IDLE     *

    // * enum state {
    //       BUSY,
    //       IDLE,
    //       ZZZZ,
    //       SICK,
    //       HURT,
    //   }
}
